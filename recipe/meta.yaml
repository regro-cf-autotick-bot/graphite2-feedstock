{% set name = "graphite2" %}
{% set version = "1.3.12" %}
{% set sha256 = "cd9530c16955c181149f9af1f13743643771cb920c75a04c95c77c871a2029d0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/silnrsi/graphite/releases/download/{{ version }}/{{ name }}-{{ version }}.tgz
  sha256: {{ sha256 }}
  patches:
    # Windows patches from https://github.com/Alexpux/MINGW-packages/tree/master/graphite2
    - 001-graphite2-1.3.12-win64.patch          # [win]
    - 002-graphite2-1.2.1-pkgconfig.patch       # [win]
    - 003-graphite2-1.3.12-staticbuild.patch    # [win]
    - 004-graphite2-1.3.12-dllimport-fix.patch  # [win]

build:
  number: 1
  detect_binary_files_with_prefix: true

requirements:
  build:
    - m2-make  # [win]
    - m2-patch  # [win]
    - cmake
    - {{ compiler('c') }}          # [not win]
    - {{ compiler('m2w64_c') }}    # [win]
    - {{ compiler('cxx') }}        # [not win]
    - {{ compiler('m2w64_cxx') }}  # [win]
  host:

test:
  commands:
    - conda inspect linkages -p $PREFIX $PKG_NAME  # [not win]
    - conda inspect objects -p $PREFIX $PKG_NAME  # [osx]

about:
  home: http://graphite.sil.org/
  license: LGPLv2
  license_file: COPYING
  summary: 'A "smart font" system that handles the complexities of lesser-known languages of the world.'

extra:
  recipe-maintainers:
    - pkgw
    - ocefpaf
